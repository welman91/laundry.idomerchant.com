// This context is persistent context, state will saved on local storage to keep state persist

import { createContext } from 'react'

const ThemeContext = createContext()

export default ThemeContext
